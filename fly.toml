app = "ssh-profile"
primary_region = "nrt"

[build]

[[services]]
  protocol = "tcp"
  internal_port = 22

  [[services.ports]]
    port = 22

  [[services.tcp_checks]]
    interval = "15s"
    timeout  = "5s"
    grace_period = "10s"

[env]
  PORT = "22"
